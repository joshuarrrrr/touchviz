# touchviz
Make sure to clone the repository with the `--recursive`-flag for the git-submodules.

## requirements
- libevdev-dev
- libglfw3-dev

## compiling
- `cmake . -B build`
- `cmake --build build`
